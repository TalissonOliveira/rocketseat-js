var nome = "Talisson" // String
var idade = 19 // Inteiro
var peso = 45.5 // Float
var humano = true // Boleano

// Vetores (armazenar vários valores)
var alunos = ['Talisson', 'Julia', 'Ryan']

// Objetos (armazenar mais de um valor referente ao objeto)
var aluno = {
    nome: alunos[0],
    idade: 19,
    peso: 45.5,
    humano: true
}
console.log(alunos[1])
console.log(aluno.peso)